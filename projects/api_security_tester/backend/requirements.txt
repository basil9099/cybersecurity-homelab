# API Security Tester — backend dependencies
# Install with: pip install -r requirements.txt

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Async HTTP client (used by all scanners)
httpx>=0.27.0

# Data validation (FastAPI depends on this but pin for clarity)
pydantic>=2.7.0
