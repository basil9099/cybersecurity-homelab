"""
ingestion — Log collection, parsing, and normalization.

This package provides collectors that watch directories or accept HTTP
input, parsers for each supported log format, and a normalizer that
converts all events into the common SENTINEL schema.
"""
